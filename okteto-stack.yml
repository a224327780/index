name: oneindex
services:
  oneindex:
    public: true
    image: caoyufei/oneindex
    replicas: 1
    environment:
      - MONGO_URI=${MONGO_URI}
      - AUTH_PASSWORD=${AUTH_PASSWORD}
    ports:
      - 8080